name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - shell: bash
        env:
          SUPER_SECRET: ${{ secrets.DOCKER_PASSWORD }}
          
        run: |
          docker login -u shivaprasad --password $SUPER_SECRET
          echo "$SUPER_SECRET"